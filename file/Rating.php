<?php
    // 내신등급 계산기 
    // 구상도 
    // 카카오톡에서 값을 받은 후 서버에서 계산 후 값을 카카오톡 채팅으로 반출
    /*
    -$a	부정	$a의 반대.
    $a + $b	덧셈	$a와 $b의 합.
    $a - $b	뺄셈	$a와 $b의 차.
    $a * $b	곱셈	$a와 $b의 곱.
    $a / $b	나눗셈	$a와 $b의 몫.
    $a % $b	나머지	$a를 $b로 나눈 나머지.
    */
function rating_all($num) {
    $num / $numall = $enterall
}

?>